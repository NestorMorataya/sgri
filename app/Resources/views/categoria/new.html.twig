{% extends 'base.html.twig' %}

{% block body %}
<hr>

<center>

<div class="container" style="margin-top:30px;">
  <div class="row well">
   <div class="col-md-6">
       <div class="page-header">
            <h2>NUEVA CATEGORÍA</h2> 
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <hr>
        <input type="submit" value="Agregar" />
    {{ form_end(form) }}
</center>

<hr>
    <ul>
        <li>
            <a href="{{ path('categoria_homepage') }}">Regresar</a>
        </li>
    </ul>
{% endblock %}
